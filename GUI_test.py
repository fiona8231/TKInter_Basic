import tkinter
from tkinter import *
from tkinter import ttk

#tkinter._test()

root = Tk()

root.title("First GUI")

ttk.Button(root, text="Hello").grid()

root.mainloop()
# ---------- MULTIPLE COMPONENTS  ----------
# Some of the different Widgets :
#  Button, Label,
# Canvas, Menu, Text, Scale, OptionMenu, Frame,
# CheckButton, LabelFrame, MenuButton, PanedWindow,
# Entry, ListBox, Message, RadioButton, ScrollBar,
# Bitmap, SpinBox, Image

